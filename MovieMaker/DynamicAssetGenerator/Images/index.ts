
export function generateImage(imagePrompt: string, outputLocation: string): boolean{ //TODO: This should be async
    //TODO: What do you do if image at outputLocation already exists?
    return true
}